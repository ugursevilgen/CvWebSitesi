<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>İletişim Formu</title>
    <!-- Font Awesome (ikonlar için) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="contact-form">
                    <h2 class="form-title text-center"><i class="fas fa-envelope me-2"></i>İletişim Formu</h2>

                    <form method="post">
                        <!-- Ad Soyad -->
                        <div class="mb-4">
                            <label for="fullName" class="form-label fw-bold">Ad Soyad <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-user"></i></span>
                                <input type="text" class="form-control" name="AdSoyad" placeholder="Adınız ve soyadınız" required>
                            </div>
                        </div>

                        <!-- Konu -->
                        <div class="mb-4">
                            <label for="subject" class="form-label fw-bold">Konu <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-tag"></i></span>
                                <input type="text" class="form-control" name="Konu" placeholder="Konu başlığı" required>
                            </div>
                        </div>

                        <!-- E-posta -->
                        <div class="mb-4">
                            <label for="email" class="form-label fw-bold">E-posta Adresi <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                                <input type="email" class="form-control" name="Mail" placeholder="ornek@mail.com" required>
                            </div>
                        </div>

                        <!-- Mesaj -->
                        <div class="mb-4">
                            <label for="message" class="form-label fw-bold">Mesajınız <span class="text-danger">*</span></label>
                            <textarea class="form-control" name="Mesaj" rows="5" placeholder="Mesajınızı buraya yazın..." required></textarea>
                        </div>

                        <!-- Gönder Butonu -->
                        <div class="text-center">
                            <button type="submit" class="btn btn-submit btn-primary text-white">
                                <i class="fas fa-paper-plane me-2"></i>Gönder
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
<!-- Bootstrap 5 JS Bundle with Popper -->

</html>